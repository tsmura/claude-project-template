# Testing Rules

When writing or modifying test files:

- Use descriptive test names that explain the expected behavior
- Follow the Arrange-Act-Assert pattern
- Mock external dependencies; never make real API calls in tests
- Aim for one assertion per test when reasonable
- Include both happy path and error cases
- Keep test files alongside or mirroring the source structure
